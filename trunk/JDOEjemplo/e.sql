CREATE TABLE EJEMPLO (
    ID_CONSECUTIVO numeric(18,0) NOT NULL,
    ID_UNO numeric(18,0) NOT NULL,
    ID_DOS numeric(18,0),
    ID_TRES numeric(5,0)
);
ALTER TABLE EJEMPLO ADD CONSTRAINT SQL090223111649330 PRIMARY KEY(ID_CONSECUTIVO);